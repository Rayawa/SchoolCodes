// 第5章例题10
// 1. 对输入字符进行编码，所有字符ASCII码向后移动4
// 2. 仅对字母进行处理；
// 注意：对于w~z的字母，+4后超过字母范围，如何处理？

#include <stdio.h>
int main()
{
	char c;
	while((c=getchar()) != '\n')
	{		
		// 思考：如何仅对字母进行处理
//		if((c>='a'&&c<='z')||(c>='A'&&c<='Z'))
//		{
//			if((c>='z'-4 && c<='z')||(c>='Z'-4 && c<='Z'))
//			{
//				c -=22;	//26-4
//			}else
//			{
//				c +=4;
//			}
//		}
		c+=4;
		printf("%c",c);
	}
	
	return 0;
}